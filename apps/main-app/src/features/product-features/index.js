/**
 * Public API of the `product-features` feature.
 *
 * Named `product-features` rather than `features` so the folder does not read
 * as `features/features/`.
 */
export { default as FeaturesPage } from './pages/FeaturesPage'
