{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter main-app build",
  "outputDirectory": "apps/main-app/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
