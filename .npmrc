# Workspace packages are consumed as source, so a peer mismatch in a transitive
# dep should warn rather than fail the whole install.
strict-peer-dependencies=false

# Keep the store strict: an app may only import what its own package.json
# declares. This is what stops one admin quietly depending on another's deps.
shamefully-hoist=false
